[pytest]
testpaths = tests
addopts = -m "not application"
markers =
    unit: fast, isolated tests with no external I/O (mocked network/LLM)
    functional: component tests exercising the FastAPI app in-process (TestClient)
    integration: tests interactions between different components (boots backend + frontend / browser)
    application: tests the entire application (backend + frontend)
